# Project Handoff - 2026-07-27

## Project
obsidian-site

## Summary
- `F:\workspace\obsidian\para`의 Markdown을 GitHub Pages에서 볼 수 있는 정적 HTML 사이트로 변환하는 첫 템플릿을 만들었다.
- Obsidian 폴더 구조를 그대로 유지하며 HTML 경로를 생성한다.
- `Area/100. 일기`도 변환 대상에 포함했다.

## Changed Files
- `README.md`: 프로젝트 목적과 운영 규칙 정리.
- `templates/page.html`: 모든 글과 폴더 index가 공유하는 기본 HTML 템플릿.
- `assets/site.css`: GitHub Pages용 기본 블로그/아카이브 스타일.
- `scripts/build_site.py`: Obsidian Markdown을 HTML로 변환하는 정적 사이트 생성기.
- `site/`: 변환된 GitHub Pages용 HTML 출력물.

## Decisions
- 원본 vault는 수정하지 않는다.
- 출력은 `F:\workspace\obsidian-site\site`에 만든다.
- `.obsidian`, `.git`, `Attached file`, `_system`은 변환에서 제외한다.
- 일기 폴더는 포함한다.
- 이미지는 아직 자동 복사하지 않고, 공개 웹페이지용 압축 이미지는 나중에 site assets로 관리한다.
- GitHub Pages에서 Jekyll 처리를 피하기 위해 `site/.nojekyll`을 생성한다.

## Result
- 변환된 Markdown 수: 577개.
- `Area/100. 일기` 포함 확인 완료.

## Next Actions
- 브라우저에서 `site/index.html`과 `site/Area/100. 일기/index.html`을 확인한다.
- 공개하면 안 되는 글이 섞였는지 점검한다.
- GitHub Pages 저장소와 커스텀 도메인 연결 방식을 정한다.
- 이미지 압축/삽입 루틴을 추가한다.
